apply plugin : 'java'

repositories {
	mavenCentral();
}

dependencies {
}

jar {
	baseName 'testdaemon'
	version '1.0'
	extension 'jar'
}

task install(type: Copy) {
	from 'build/libs'
	into 'testproduct/lib'
}
